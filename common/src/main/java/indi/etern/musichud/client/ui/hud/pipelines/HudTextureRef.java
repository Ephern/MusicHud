package indi.etern.musichud.client.ui.hud.pipelines;

/**
 * Opaque handle to a GPU texture, decoupled from the platform texture view type so the
 * neutral HUD layer does not depend on Minecraft's texture API.
 */
public interface HudTextureRef {
}
